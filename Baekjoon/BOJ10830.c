#include <stdio.h>

void matrix_multiply(int arr[], int n);

int main(void){
    int N, B;
    scanf("%d %d", &N, &B);

    int *arr[N*N];
    for(int i = 0; i < N*N; i++){
        scanf("%d", &arr[i]);
    }


}

void matrix_multiply(int arr[], int n){
    int new_arr[] = arr[];
    
}