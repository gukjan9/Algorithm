//BOJ18429
#include <stdio.h>
#define SIZE 8

int arr[SIZE];                  // 운동기구별 운동증가량

int main(void){
    int init = 500;
    int N, K;                   // N = 일 수, K = 하루 근손실양
    scanf("%d %d", &N, &K);

    for(int i = 0; i < N; i++)
        scanf("%d", arr[i]);

    
}